---
title: "Module assessment"
url: "https://learn.microsoft.com/en-us/training/modules/understand-features-copilot-studio-agents/knowledge-check"
uid: "learn-bizapps.understand-features-copilot-studio-agents.knowledge-check"
module: "understand-features-copilot-studio-agents"
moduleTitle: "Understand features of Copilot Studio agents"
learningPath: "learn.wwl.pl-900-power-platform-fundamentals"
---
# Module assessment

1.

Contoso's operations team needs an agent that monitors their inventory system every night, identifies products below the reorder threshold, and automatically places purchase orders with approved suppliers without human intervention. Which type of agent best fits this scenario?

A conversational agent connected to a SharePoint knowledge source

An autonomous agent with agent flows that trigger on a schedule and call connector actions

A conversational agent with MCP server tools for reading inventory data

2.

A maker wants to confirm that the data employees share with a Copilot Studio agent is not used to train Microsoft's foundation AI models. Which Microsoft commitment addresses this directly?

Geographic data residency settings in the Power Platform admin center

Microsoft's commitment that prompts, responses, and accessed data are not used to train foundation models

Power Platform DLP policies that block external connectors

3.

In the Copilot Studio data flow, what happens between the step where the user sends a message and the step where the AI model generates a response?

The agent publishes the response to the configured channel

Topic or generative orchestration evaluates the input, retrieves knowledge, and executes any needed tools

The agent runs an agent evaluation test set against the user's message

4.

Contoso's HR team wants the Benefits Advisor agent to always follow an exact sequence of steps when processing a benefits enrollment request—collecting plan selection, confirming dependent information, verifying eligibility, and submitting to the HR system—with no AI reasoning variation. Which capability is most appropriate?

Generative orchestration with a Dataverse knowledge source

An agent flow that defines the exact enrollment steps as a deterministic workflow

A multi-turn conversation evaluation test set

5.

An agent is configured to look up customer account details from a Dataverse MCP server. An administrator updates the DLP policy to block the Dataverse connector. What happens to the agent?

The agent continues to work because MCP servers bypass DLP policies

The agent cannot use the Dataverse MCP server tools because the underlying connector is blocked by the DLP policy

The agent automatically switches to reading Dataverse data through SharePoint instead

You must answer all questions before checking your work.

You must answer all questions before checking your work.
